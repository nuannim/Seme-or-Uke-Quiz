<!-- LANGUAGE SELECTOR -->
<div align="center">
  <a href="#english">English</a> | <a href="#thai">ภาษาไทย</a>
</div>

<hr />

<a id="english"></a>
## Installation (English)

This project has no server-side database or secrets to back up — everything needed to run it lives in this Git repository (source code, quiz questions/results/song data in `public/data/`, and images in `public/images/`). If you ever delete this folder, this guide will get you back to exactly where you are now.

### Prerequisites

- **Git**
- **Node.js** (includes npm). Any recent LTS version works — this was last set up with `Node v22.16.0` / `npm 10.9.2`.

### 1. Get the source code

```sh
git clone https://github.com/nuannim/Seme-or-Uke-Quiz.git
cd Seme-or-Uke-Quiz
```

Everything is already tracked in git — quiz content (`public/data/qna3.js`, `result.js`, `song.js`), images, `index.html`, `app.js`, `style.css`. Nothing needs to be copied back manually.

### 2. Install dependencies

```sh
npm install
```

This reads `package-lock.json` and installs the only dependency, Vite, into a fresh `node_modules/` folder.

### 3. Run it

| Command | What it does |
|---|---|
| `npm run dev` | Starts the local dev server (Vite prints the URL, e.g. `http://localhost:5173`). Use this for day-to-day editing. |
| `npm run build` | Builds a production bundle into `dist/`. Only needed when deploying (e.g. Vercel, GitHub Pages). |
| `npm run preview` | Serves the built `dist/` locally, to sanity-check a production build before deploying. |

### Notes

- `node_modules/` and `dist/` are intentionally excluded from git (see `.gitignore`) — they're regenerated by the commands above, so there's nothing to back up there.
- If you want project-specific guidance for working on the code (architecture, conventions, etc.), see `CLAUDE.md`.

<p align="right">(<a href="#readme-top">back to top</a>)</p>

<hr />

<a id="thai"></a>
## วิธีติดตั้ง (ภาษาไทย)

โปรเจกต์นี้ไม่มีฐานข้อมูลฝั่งเซิร์ฟเวอร์หรือความลับใด ๆ ที่ต้องสำรองแยกต่างหาก ทุกอย่างที่ต้องใช้รันเว็บนี้อยู่ใน Git repository แล้ว (โค้ด, ข้อมูลคำถาม/ผลลัพธ์/เพลงใน `public/data/`, และรูปภาพใน `public/images/`) ถ้าลบโฟลเดอร์นี้ไปในอนาคต ทำตามขั้นตอนนี้แล้วกลับมารันได้เหมือนเดิมทุกอย่าง

### สิ่งที่ต้องมีก่อนติดตั้ง

- **Git**
- **Node.js** (มี npm มาให้ในตัว) ใช้เวอร์ชัน LTS ล่าสุดได้เลย — ตอนตั้งค่าล่าสุดใช้ `Node v22.16.0` / `npm 10.9.2`

### 1. ดึงโค้ดกลับมา

```sh
git clone https://github.com/nuannim/Seme-or-Uke-Quiz.git
cd Seme-or-Uke-Quiz
```

ทุกไฟล์ที่จำเป็นถูก track ไว้ใน git อยู่แล้ว ทั้งข้อมูลแบบทดสอบ (`public/data/qna3.js`, `result.js`, `song.js`), รูปภาพ, `index.html`, `app.js`, `style.css` ไม่ต้องก็อปปี้อะไรกลับมาเองเพิ่ม

### 2. ติดตั้ง dependencies

```sh
npm install
```

คำสั่งนี้จะอ่านจาก `package-lock.json` แล้วติดตั้ง dependency เดียวที่โปรเจกต์ใช้คือ Vite ลงในโฟลเดอร์ `node_modules/` ใหม่

### 3. รันโปรเจกต์

| คำสั่ง | ใช้ทำอะไร |
|---|---|
| `npm run dev` | รัน dev server ในเครื่อง (Vite จะขึ้น URL ให้ เช่น `http://localhost:5173`) ใช้ตอนแก้โค้ดทั่วไป |
| `npm run build` | บิลด์เวอร์ชัน production ลงโฟลเดอร์ `dist/` ใช้ตอนจะ deploy เท่านั้น (เช่น Vercel, GitHub Pages) |
| `npm run preview` | เปิดดูไฟล์ที่ build แล้วใน `dist/` ในเครื่อง เพื่อเช็คก่อน deploy จริง |

### หมายเหตุ

- `node_modules/` และ `dist/` ถูกตั้งใจไม่ให้ git track (ดูใน `.gitignore`) เพราะสร้างขึ้นใหม่ได้จากคำสั่งด้านบน ไม่ต้องสำรองไว้
- ถ้าต้องการรายละเอียดเชิงโครงสร้างโค้ด/แนวทางการแก้ไข ดูที่ไฟล์ `CLAUDE.md`

<p align="right">(<a href="#readme-top">กลับสู่ด้านบน</a>)</p>
